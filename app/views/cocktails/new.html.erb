<div class="wra">
  <h1>new cocktail</h1>
</div>

  <div class="center">
  <%= simple_form_for @cocktail do |f| %>
    <%= f.input :name %>
    <%= f.input :photo %>
    <%= f.input :photo_cache, as: :hidden %>
    <%= f.button :submit %>
  <% end %>
  <%= link_to "back to the list", cocktails_path  %>
</div>
